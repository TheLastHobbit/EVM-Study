{
  "name": "nftmarkettest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nftmarkettest",
    "create-local": "graph create --node http://localhost:8020/ nftmarkettest",
    "remove-local": "graph remove --node http://localhost:8020/ nftmarkettest",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nftmarkettest",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
